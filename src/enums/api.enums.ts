export enum APIState {
    Idle = "Idle",
    Fetching = "Fetching",
    Fetched = "Fetched"
}

export enum APIResource {
    Pokemon = "pokemon",
    Type = "type",
    Move = "move",
    Ability = "ability",
    Item = "item",
    Location = "location",
}
